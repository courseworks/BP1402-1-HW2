// src/add_two_numbers.c
#include <stdio.h>
#ifndef __cplusplus
typedef unsigned char bool;
static const bool false = 0;
static const bool true = 1;
#endif

// Check whether a number is prime or not
bool isPrime(unsigned long n) {
    unsigned long i;
    bool flag = true;

    /*
    TODO: Write the implementation here!

    HINT: If number is not prime, then assign the flag to false
    */
 
    return flag;
}

// Print prime numbers between two integers
void printPrimesBetweenTwoInts(unsigned long start, unsigned long end) {
    unsigned long i;

    /*
    TODO: Write the implementation here!

    HINT: iterate from start to end (interval),
    pass each number to isPrime function,
    if returned true, print the number.
    */

}   